#ifndef dijkstra
#define dijkstra

#include <map>
#include <string>
#include <vector>
#include "node.hpp"

std::vector<Node>* findPath(std::map<std::string, std::vector<Node>>*, Node*, Node*);

#endif